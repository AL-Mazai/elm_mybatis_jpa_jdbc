Mybatis：
    导入依赖
    配置文件
    util层
    接口（包含与xml文件里SQL语句对应的java方法）和 xml文件相互映射（包含SQL语句和映射规则）
        {映射规则指的是将查询结果映射到Java对象属性的规则。通过映射规则，可以将SQL查询结果转换为Java对象。}
    接口实现类
    xml文件中书写SQL语句
获取sqlSessionFactory对象——>获取sqlSession对象——>获取接口的代理对象(Mapper对象)——>执行增删改查——>释放资源

JPA：
    导入依赖
    配置文件
    util层
    接口
    接口实现类（SQL语句在实现类中编写）

JDBC：
    导入依赖
    配置文件
    util层
        创建配置文件类conf--->找到配置文件路径--->将将配置文件里的属性加载到conf类里--->使用反射获取driver加载驱动--->建立连接...
    接口
    接口实现类

